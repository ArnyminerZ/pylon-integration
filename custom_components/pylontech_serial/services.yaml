send_command:
  name: Send Command
  description: Sends a raw command to the Pylontech BMS and returns the response.
  fields:
    command:
      name: Command
      description: The ASCII command to send (e.g., 'pwr', 'help', 'info').
      required: true
      selector:
        text:
  target: {}
